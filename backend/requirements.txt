# EvidenceOS PRIME - Python Backend Requirements

# Core framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic[email]==2.5.0

# Data processing
pandas==2.1.3
numpy==1.26.2
openpyxl==3.1.2  # Excel support

# JSON handling
orjson==3.9.10

# Authentication & Security
python-jose[cryptography]==3.3.0  # JWT tokens
passlib[bcrypt]==1.7.4  # Password hashing
python-multipart==0.0.6  # Form data parsing
bcrypt==4.1.2  # Password hashing backend

# Rate limiting
slowapi==0.1.9

# Caching (Parquet support)
pyarrow==14.0.1

# Environment variables
python-dotenv==1.0.0

# Database (PostgreSQL)
psycopg2-binary==2.9.9  # PostgreSQL adapter
sqlalchemy==2.0.23  # ORM
alembic==1.13.1  # Database migrations

# Redis (distributed caching)
redis==5.0.1
hiredis==2.3.2  # Redis performance boost

# Monitoring & Observability
prometheus-client==0.19.0  # Metrics
sentry-sdk[fastapi]==1.38.0  # Error tracking

# Logging
python-json-logger==2.0.7  # Structured logging

# HTTP client
httpx==0.25.2  # Async HTTP client

# Machine Learning & AI - Core
scikit-learn==1.3.2  # ML models (Random Forest, Gradient Boosting)
scipy==1.11.4  # Scientific computing (stats, optimization)
joblib==1.3.2  # Model persistence

# World-Class Gradient Boosting Frameworks
xgboost==2.0.3  # XGBoost - best for regularization & missing values
lightgbm==4.1.0  # LightGBM - fastest, best for large datasets
catboost==1.2.2  # CatBoost - best for categorical features

# Explainable AI (Critical for Healthcare ML)
shap==0.44.0  # SHAP - theoretically grounded global/local explanations
lime==0.2.0.1  # LIME - model-agnostic interpretability

# AutoML & Hyperparameter Optimization
optuna==3.5.0  # Optuna - state-of-the-art Bayesian optimization

# MLOps Infrastructure (Production-Grade)
mlflow==2.9.2  # Experiment tracking & model registry
evidently==0.4.12  # Drift detection & model monitoring

# RAG System (Retrieval-Augmented Generation)
chromadb==0.4.22  # Vector database for semantic search
sentence-transformers==2.3.1  # Neural embeddings for RAG

# Local LLM Integration (NO external APIs)
llama-cpp-python==0.2.20  # Local Llama 3 models (CPU/GPU)

# Optional: Advanced features
# transformers==4.36.0  # HuggingFace transformers (for advanced NLP)
# pymc==5.10.0  # Bayesian analysis
# arviz==0.17.0  # Bayesian visualization

# Testing
pytest==7.4.3
pytest-cov==4.1.0
pytest-asyncio==0.21.1  # Async test support
pytest-mock==3.12.0  # Mocking
faker==20.1.0  # Test data generation

# Code quality
black==23.11.0
flake8==6.1.0
mypy==1.7.1
pylint==3.0.3
isort==5.13.2  # Import sorting
bandit==1.7.5  # Security linting

# Pre-commit hooks
pre-commit==3.6.0

# Documentation
mkdocs==1.5.3  # Documentation generator
mkdocs-material==9.5.3  # Material theme
