apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: evidenceos-data-pvc
  namespace: evidenceos
  labels:
    app: evidenceos
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: fast-ssd
